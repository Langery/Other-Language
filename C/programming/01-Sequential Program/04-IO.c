#include <stdio.h>
#include <math.h>

int main() {
  /*
  double a,b,c, disc, xl, x2, p, q;
  scanf("%f%f%f", &a, &b, &c);
  disc = b * b - 4 - a * c;
  p = -b / (2.0 * a);
  q = sqrt(disc) / (2.0 * a);
  xl = p + q;
  x2 = p - q;
  printf("x1 = %2f\nx2 = %2f\n", xl, x2);
  return 0;
  */
}